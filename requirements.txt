# Core Discord Bot Dependencies
discord.py>=2.4.0
aiohttp>=3.10.11,<4  # 3.9.1 had known CVEs; 3.10.11+ patched, still discord.py-compatible
python-dotenv==1.0.0
pytz>=2024.1
PyNaCl>=1.5.0

# College Football Data API
cfbd>=5.0.0

# Dashboard Dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.2
httpx>=0.26.0
python-multipart>=0.0.6
itsdangerous>=2.1.2

# High School Stats Scraper (optional)
beautifulsoup4>=4.12.0

# Cloudflare bypass for web scraping
cloudscraper>=1.2.71
playwright>=1.40.0
zyte-api>=0.5.0

# Fuzzy string matching for player name typos
rapidfuzz>=3.0.0

# Optional AI Integration
# AI calls go over raw HTTP (see ai/ai_integration.py) — no vendor SDKs needed

# Optional Monitoring & Error Tracking

# Optional Google Docs Integration

# Development Dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
